# {{ ansible_managed }}

server {
	listen {{ item.port }};
    server_name {{ item.domains|join(' ') }};
{% for v in item.setup %}
   {{ v.replace(";",";\n      ").replace("{","{\n      ").replace("}","\n   }\n") }}{% if v.find('{') == -1%};{% endif %} 
{% endfor %}

}
